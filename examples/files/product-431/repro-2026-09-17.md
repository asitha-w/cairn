# Reproduction: double order on client retry

Throttle the client to 3G in the browser tools, submit an order, wait for the spinner. The client retries at 8 s; the first request commits at 9 s. Two orders, identical payload, 1.2 s apart.

Request ids and timestamps from staging follow.
