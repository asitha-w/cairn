#!/usr/bin/env bash
# crn — Cairn CLI entry point. The code is the crn/ package next to this file.
exec env PYTHONPATH="$(cd "$(dirname "$(readlink -f "$0")")/.." && pwd)${PYTHONPATH:+:$PYTHONPATH}" python3 -m crn.cli "$@"
