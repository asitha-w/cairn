---
description: Open a Cairn work node with zero tool calls — the node is injected into context before the model answers.
argument-hint: <key | number | slug>
---

Node requested: $ARGUMENTS

!`crn open $ARGUMENTS 2>&1 | head -120`

Read Now and Next above and state the resume point in three lines: situation, what it waits on, next step. Do not open other nodes.
